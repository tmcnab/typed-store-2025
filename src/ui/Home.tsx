export default function Home() {
	return (
		<fieldset>
			<legend>Main</legend>
			<p>Welcome to TypedStore, a storage-agnostic data store that is a cross between a JSON blob store and RethinkDB.</p>
		</fieldset>
	)
}